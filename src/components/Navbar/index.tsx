import styles from './Navbar.module.css';

const Navbar = () => {

    return (
        <div className= {styles.wrapper}>
        <p className= {styles.text}>Weather Forecast App</p>
        </div>

    )
}

export default Navbar